where
	Javascript: $where, $regex
	Element: $exists, $mod, $type
	
	

update references: when root object changes, the references also changes
selected references: i want to only cache the title of the user. so only when the title changes it the original object, it should change
deleting single
find all
deleting with where
updating with where
bulk update
bulk delete
select
indexing

write tests

break out from each loop



done
----
where
select
updating
clone object in reference
where
	deep compare: done
	comparison: $ne,$lt,$lte,$gt,$gte,$in,$ni,$all
	shortcut comparison: "x.y": 2, instead of x:{y:2}
	logical: $or,$and, $not, $nor
	Array: $elemMatch, $size
	
